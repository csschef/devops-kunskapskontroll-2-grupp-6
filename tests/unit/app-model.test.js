import { describe, test, expect, beforeEach } from "vitest";
import { AppModel } from "../../src/app-model.js";

describe("app-model", () => {
	test("has a basic smoke test", () => {
		expect(true).toBe(true);
	});
});
